#! worker-storage-vol-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: worker-storage-vol
    namespace: faceless-video-app
    labels:
        com.docker.compose.service: worker
        com.docker.compose.volume: storage_vol
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
